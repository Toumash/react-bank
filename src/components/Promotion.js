import React from 'react';

const Promotion = ({title, bank, image}) => {
    return (
        <div>
            <h1>{title}</h1>
            Component!
        </div>
    );
};

export default Promotion;